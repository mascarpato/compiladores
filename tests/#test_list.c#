#include <stdio.h>
#include <assert.h>
#include "../include/comp_list.h"

int vector[] = {0, 1, 2, 3, 4, 5};

int main() {
  ListNode *list;
  int NewStrangeVariable;

  list = list_create();
  assert(list == NULL);

  assert(list_contains(list, vector[0]) == 0);
}
  
